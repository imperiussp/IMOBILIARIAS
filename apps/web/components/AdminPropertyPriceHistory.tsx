"use client";

import { useEffect, useState } from "react";
import { getCurrentAgency } from "../lib/currentAgency";
import { isSupabaseConfigured, supabaseBrowser } from "../lib/supabaseBrowser";

type Row={property_id:string;code:string;title:string;previous_price:number;price:number;changed_at:string;change_percent:number};
export default function AdminPropertyPriceHistory(){const [rows,setRows]=useState<Row[]>([]);const [message,setMessage]=useState("");useEffect(()=>{void(async()=>{if(!supabaseBrowser||!isSupabaseConfigured)return;const a=await getCurrentAgency();if(!a)return setMessage("Imobiliária ativa não encontrada.");const r=await supabaseBrowser.from("agency_recent_price_changes").select("property_id,code,title,previous_price,price,changed_at,change_percent").eq("agency_id",a.agencyId).order("changed_at",{ascending:false}).limit(100);if(r.error&&!['42P01'].includes(r.error.code||''))return setMessage(r.error.message);setRows((r.data||[]) as Row[]);})();},[]);const money=(v:number)=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});return <div className="adminPanel" id="historico-precos"><div className="adminPanelHeader"><div><span className="eyebrow">PREÇOS</span><h2>Histórico de preços</h2><p>Acompanhe alterações de valor dos imóveis sem perder o preço anterior.</p></div><span>{rows.length} alteração(ões)</span></div>{rows.length?<div className="adminTableWrap"><table className="adminTable"><thead><tr><th>Imóvel</th><th>Anterior</th><th>Novo</th><th>Variação</th><th>Data</th></tr></thead><tbody>{rows.map((r,i)=><tr key={`${r.property_id}-${r.changed_at}-${i}`}><td><strong>{r.code}</strong><small className="tableSub">{r.title}</small></td><td>{money(r.previous_price)}</td><td>{money(r.price)}</td><td><span className="statusPill">{Number(r.change_percent||0)>0?'+':''}{Number(r.change_percent||0).toLocaleString('pt-BR')}%</span></td><td>{new Date(r.changed_at).toLocaleString('pt-BR')}</td></tr>)}</tbody></table></div>:isSupabaseConfigured?<div className="formNotice">As mudanças de preço aparecerão aqui automaticamente.</div>:null}{message?<div className="formMessage">{message}</div>:null}</div>}
